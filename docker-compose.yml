version: "3.8"

services:
  sdk-runner:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - RPC_URL=https://rpc.buildbear.io/sweet-gorgon-268627e3
      - MNEMONIC=guilt clock kingdom banana margin rich junk swap during crane staff creek
      - VERBOSE_LEVEL=3
      - VERBOSE=true
    volumes:
      # Optional: Mount a volume to persist any generated files
      - ./output:/app/sdk-branch-192/output
    # Override the default command if needed
    # command: ["pnpm", "cli", "fellowship", "game", "create", "0x4D3A53F1c86b7BfFD95130d469B898f06B3Eb038", "-i", "1"]

    # For interactive mode (if needed for debugging)
    # stdin_open: true
    # tty: true
